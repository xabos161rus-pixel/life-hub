/** Всё про установку приложения в одном месте — используется в окне
 *  переустановки, в Настройках и в инструкции. */

/** Адрес сайта для установки: origin + базовый путь сборки (/life-hub/).
 *  Берём из BASE_URL, чтобы ссылка оставалась верной при смене домена. */
export const INSTALL_URL = new URL(import.meta.env.BASE_URL || '/', window.location.origin).href;

/**
 * Версия окна о переустановке. Меняется, когда окно нужно ПОКАЗАТЬ заново всем
 * (например, снова обновили значок). Пользователь видит окно, пока его
 * settings.reinstallNoticeSeen не равен этой версии; при закрытии сюда
 * записывается текущая версия. Старые значения (ISO-время прошлого закрытия)
 * версии не равны — поэтому окно покажется ещё раз, что и требуется.
 */
export const REINSTALL_NOTICE_VERSION = '2026-07-b';
