/** Пресеты цветов для проектов, целей, задач и метрик. */
export const PRESET_COLORS = [
  '#7c9aff', // синий
  '#5b8def', // индиго
  '#22d3ee', // циан
  '#2dd4bf', // бирюзовый
  '#4ade80', // зелёный
  '#a3e635', // лаймовый
  '#fbbf24', // янтарный
  '#fb923c', // оранжевый
  '#f87171', // красный
  '#fb7185', // коралловый
  '#f472b6', // розовый
  '#e879f9', // фуксия
  '#c084fc', // фиолетовый
  '#a78bfa', // лавандовый
  '#94a3b8', // серый
  '#d4a373', // песочный
  '#6366f1', // электрик-индиго
  '#0ea5e9', // небесный
  '#14b8a6', // тил
  '#10b981', // изумруд
  '#eab308', // золото
  '#ef4444', // алый
  '#ec4899', // маджента
  '#8b5cf6', // фиолет
  '#0891b2', // глубокий циан
  '#78716c', // камень
  '#ffffff', // белый
];

/** Светлый ли цвет (для выбора тёмной галочки/текста поверх — иначе белое на белом). */
export function isLightColor(hex: string): boolean {
  const h = hex.replace('#', '');
  if (h.length < 6) return false;
  const r = parseInt(h.slice(0, 2), 16);
  const g = parseInt(h.slice(2, 4), 16);
  const b = parseInt(h.slice(4, 6), 16);
  return 0.299 * r + 0.587 * g + 0.114 * b > 186; // перцептивная яркость sRGB
}

/** Контрастный «штамп» поверх заливки этим цветом (галочка в чекбоксе и т.п.). */
export const ON_COLOR_DARK = '#0e0f17';
